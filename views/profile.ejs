<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Login Form - Dark Mode</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body
    class="bg-gray-900 flex flex-col items-center justify-center min-h-screen relative mt-4 mb-4"
  >
    <div class="absolute top-4 right-4">
      <a href="/logout">
        <button
          class="bg-red-600 text-white font-semibold py-2 px-4 rounded-md hover:bg-red-700 transition duration-200"
        >
          Logout
        </button>
      </a>
    </div>

    <h1 class="text-white text-2xl font-bold mb-4">
      Hello, <%= user.username %>
    </h1>

    <div class="bg-gray-800 rounded-lg p-6 w-full max-w-md">
      <p class="text-gray-400 mb-4">You can write posts here</p>
      <form action="/post" method="POST" class="mb-4">
        <textarea
          name="content"
          id="content"
          class="bg-gray-700 text-white rounded-lg p-2 w-full h-20 focus:outline-none focus:ring focus:ring-blue-500 resize-none"
          placeholder="Write your post..."
          required
        ></textarea>
        <button
          type="submit"
          class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-lg mt-2"
        >
          Submit
        </button>
      </form>
      <div>
        <h2 class="text-white font-bold mb-2">Your posts</h2>
        <!--  -->
        <% user.posts.reverse().forEach((post)=>{ %>
        <div class="bg-gray-700 rounded-lg p-4 mb-2">
          <h4 class="text-blue-500 font-bold mb-1">@<%= user.username %></h4>
          <p class="text-gray-400"><%= post.content %></p>
          <div class="flex space-x-4 mt-2">
            <a
              href=""
              class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-1 px-2 text-sm rounded-md transition duration-200"
            >
              Like
            </a>
            <a
              href=""
              class="bg-yellow-500 hover:bg-yellow-600 text-white font-semibold py-1 px-2 text-sm rounded-md transition duration-200"
            >
              Edit
            </a>
          </div>
        </div>
        <% }) %>
        <!--  -->
      </div>
    </div>
  </body>
</html>
